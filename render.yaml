services:
  - type: web
    name: biokey-api
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get install -y libgl1-mesa-glx
    startCommand: python api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0