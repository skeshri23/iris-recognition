services:
  - type: web
    name: biokey-api
    runtime: python
    buildCommand: apt-get install -y libgl1-mesa-glx libgles2-mesa && pip install -r requirements.txt
    startCommand: python api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0